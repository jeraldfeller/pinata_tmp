{% extends '@AppFaq/Admin/layout.html.twig' %}

{% block title 'Edit Category' %}

{% block breadcrumb_crumbs %}
    {{ parent() }}
    <li><a href="{{ path('app_faq.admin.category.index') }}">Categories</a> <span class="divider">/</span></li>
    <li><a href="{{ path('app_faq.admin.category.update', {'id': category.id}) }}">{{ category.title }}</a> <span class="divider">/</span></li>
    <li class="active">Edit Category</li>
{% endblock breadcrumb_crumbs %}

{% block content %}
    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
        {% include '@AppFaq/Admin/Category/_form.html.twig' with {'form': form, 'category': category} %}
        <div class="form-actions">
            <button type="submit" class="btn btn-primary"><i class="icon-pencil icon-white"></i> Save Category</button>
            <a href="{{ path('app_faq.admin.category.index') }}" class="btn"><i class="icon-refresh"></i> Cancel</a>
        </div>
    {{ form_end(form) }}
{% endblock content %}
